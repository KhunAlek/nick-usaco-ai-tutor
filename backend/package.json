{
  "name": "nick-usaco-ai-tutor-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply nick-usaco-tutor-db --local",
    "db:remote": "wrangler d1 migrations apply nick-usaco-tutor-db --remote",
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.24.3"
  }
}
